# CLI arguments for pipeline/harvest.mjs, one per line, # for comments.
# run 4: the first run that should actually land. The previous three were
# killed by cancel-in-progress or lost their commit to a push race; both are
# fixed, and the corpus is now uploaded as an artifact regardless.
