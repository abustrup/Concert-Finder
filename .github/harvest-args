# CLI arguments for pipeline/harvest.mjs, one per line, # for comments.
# run 3: Danish compound filtering, postponed shows dropped, VEGA walks its
# sitemap, Roskilde parked. First run that also enriches artists.
