# CLI arguments for pipeline/harvest.mjs, one per line, # for comments.
# run 2: next-data adapter for VEGA/Roskilde, Danish date parsing for WordPress,
# page fallback for venues whose API record carries no date. Full limits.
