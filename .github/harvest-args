# CLI arguments for pipeline/harvest.mjs, one per line, # for comments.
# Touch this file to trigger a run on a feature branch.
# run 1: smoke test - do the adapters work at all against real sites?
--limit
8
